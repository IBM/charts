# begin_generated_IBM_copyright_prolog                             
#                                                                  
# This is an automatically generated copyright prolog.             
# After initializing,  DO NOT MODIFY OR MOVE                       
# **************************************************************** 
# Licensed Materials - Property of IBM                             
# 5724-Y95                                                         
# (C) Copyright IBM Corp.  2019, 2019    All Rights Reserved.      
# US Government Users Restricted Rights - Use, duplication or      
# disclosure restricted by GSA ADP Schedule Contract with          
# IBM Corp.                                                        
#                                                                  
# end_generated_IBM_copyright_prolog                               
apiVersion: v1
kind: Service
metadata:
  name: {{ template "zookeeper.fullname" . }}-hs
  labels:
    app.kubernetes.io/name: {{ template "zookeeper.name" . }}
{{- include "zookeeper.defaultLabels" . | indent 4 }}
{{- include "zookeeper.serviceability" . | indent 4 }}

spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: client
      port: {{ .Values.service.port | default 2181 }}
      targetPort: client
      protocol: {{ .Values.service.protocol | default "TCP" }}
    - name: server
      port: {{ .Values.service.serverPort | default 2888 }}
      targetPort: server
      protocol: {{ .Values.service.protocol | default "TCP" }}
    - name: election
      port: {{ .Values.service.electionPort | default 3888 }}
      targetPort: election
      protocol: {{ .Values.service.protocol | default "TCP" }}
  selector:
    app.kubernetes.io/name: {{ template "zookeeper.name" . }}
    release: {{ .Release.Name | quote }}
