# begin_generated_IBM_copyright_prolog                             
#                                                                  
# This is an automatically generated copyright prolog.             
# After initializing,  DO NOT MODIFY OR MOVE                       
# **************************************************************** 
# Licensed Materials - Property of IBM                             
# 5724-Y95                                                         
# (C) Copyright IBM Corp.  2019, 2019    All Rights Reserved.      
# US Government Users Restricted Rights - Use, duplication or      
# disclosure restricted by GSA ADP Schedule Contract with          
# IBM Corp.                                                        
#                                                                  
# end_generated_IBM_copyright_prolog    
apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-zookeeper-test"
  labels:
{{- include "zookeeper.defaultLabels" . | indent 4 }}
    app.kubernetes.io/name: "{{ .Release.Name }}-zookeeper-test"  
  annotations:
    "helm.sh/hook": test-success
spec:
  affinity:
  {{- include "zookeeper.nodeaffinity" . | indent 4 }}
  serviceAccountName: {{ template "zookeeper.serviceAccountName" . }}
{{- include "zookeeper.podGeneralSecurityPolicies" . | indent 2 }}
  securityContext:
    runAsUser: {{ template "zookeeper.streamsinstall" . }}
    fsGroup: {{ template "zookeeper.streamsinstall" . }}
{{- include "zookeeper.podSecurityContext" . | indent 4 }}
  containers:
  - name: {{ .Release.Name }}-zookeeper-test
    image: {{ .Values.image.prefix }}/{{ .Values.image.repository }}:{{ .Values.image.tag }} 
    imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
    securityContext:
{{- include "zookeeper.containerSecurityContext" . | indent 6 }}

    env:
      - name: LANG
        value: "en_US.utf-8"
 
    command: ["bash", "-c", "set -x ; exit 0 ; "]

    resources:
      requests:
        cpu: "1"
        memory: "1Gi"
      limits:
        cpu: "2" 
        memory: "1Gi"    

  restartPolicy: Never
