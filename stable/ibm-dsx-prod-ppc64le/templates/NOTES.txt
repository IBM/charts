Data Science Experience (DSX) is delivered as an interconnected set of pods and kubernetes services. DSX Local on Power supports one installation per cluster.
A unique node port must be selected to be able to access the DSX web application.
{{ if not (eq .Release.Namespace .Values.dsxNamespaces.ibmPrivateCloud) -}}
If you are following the official documentation your next step must be:
	{{ if eq .Release.Namespace .Values.dsxNamespaces.sysIbmAdmData -}}
		helm install --namespace sysibm-adm --name dsxns2 stable/ibm-dsx-prod-ppc64le
	{{ else if eq .Release.Namespace .Values.dsxNamespaces.sysIbmAdm }}
		helm install --namespace dsxl-ml --name dsxns3 stable/ibm-dsx-prod-ppc64le
	{{ else if eq .Release.Namespace .Values.dsxNamespaces.dsxlMl }}
  # "namespace" omitted since ClusterRoles are not namespaced
		helm install --namespace ibm-private-cloud --name dsxns4 stable/ibm-dsx-prod-ppc64le --set dsxservice.externalPort=<a node port>
	{{- end }}
{{ else }}
When the ibm-nginx deployment is ready, use https://<external ip>:{{ .Values.dsxservice.externalPort }} to access the application. Default login is: admin/password
{{- end }}
