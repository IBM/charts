{{ if (eq .Release.Namespace .Values.dsxNamespaces.sysIbmAdm) -}}
apiVersion: v1
kind: Service
metadata:
  name: dash-front-svc
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    run: dash-front-svc
    component: dash-front
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3000
      protocol: TCP
{{- end }}