{{ if (eq .Release.Namespace .Values.dsxNamespaces.dsxlMl) -}}
apiVersion: v1
kind: Service
metadata:
  name: mldeployment-z-svc
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    run: mldeployment-z-svc
    component: mldeployment-z
spec:
  type: ClusterIP
  ports:
    - port: 14150
      targetPort: 14150
      protocol: TCP
{{- end }}