nfspluginImage:
  __metadata:
    name: "define nfspluginImage"
    label: "define nfspluginImage"
    description: "define image repository, pullPolicy and tag for nfspluginImage"
  repository:
    __metadata:
      name: "repository"
      label: "repository"
      description: "Docker image location"
      type: "string"
      immutable: false
      required: true
  pullPolicy:
    __metadata:
      name: "pullPolicy"
      label: "Docker image pull policy"
      description: "Always, Never, or IfNotPresent. Defaults to IfNotPresent"
      type: "string"
      immutable: false
      required: true
      options:
      - label: "Always"
        value: "Always"
      - label: "Never"
        value: "Never"
      - label: "IfNotPresent"
        value: "IfNotPresent"  
  tag: 
    __metadata:
      name: "Docker image Tag"
      label: "Docker image Tag"
      description: "nfspluginImage's Tag"
      type: "string"
      immutable: false
      required: true

attacherImage:
  __metadata:
    name: "define attacherImage"
    label: "define attacherImage"
    description: "define image repository, pullPolicy and tag for attacherImage"
  repository:
    __metadata:
      name: "repository"
      label: "repository"
      description: "Docker image location"
      type: "string"
      immutable: false
      required: true
  pullPolicy:
    __metadata:
      name: "pullPolicy"
      label: "Docker image pull policy"
      description: "Always, Never, or IfNotPresent. Defaults to IfNotPresent"
      type: "string"
      immutable: false
      required: true
      options:
      - label: "Always"
        value: "Always"
      - label: "Never"
        value: "Never"
      - label: "IfNotPresent"
        value: "IfNotPresent"  
  tag: 
    __metadata:
      name: "Docker image Tag"
      label: "Docker image Tag"
      description: "attacherImage's Tag"
      immutable: false
      required: true

registerImage:
  __metadata:
    name: "define registerImage"
    label: "define registerImage"
    description: "define image repository, pullPolicy and tag for registerImage"
  repository:
    __metadata:
      name: "repository"
      label: "repository"
      description: "Docker image location"
      type: "string"
      immutable: false
      required: true
  pullPolicy:
    __metadata:
      name: "pullPolicy"
      label: "Docker image pull policy"
      description: "Always, Never, or IfNotPresent. Defaults to IfNotPresent"
      type: "string"
      immutable: false
      required: true
      options:
      - label: "Always"
        value: "Always"
      - label: "Never"
        value: "Never"
      - label: "IfNotPresent"
        value: "IfNotPresent"  
  tag: 
    __metadata:
      name: "Docker image Tag"
      label: "Docker image Tag"
      description: "registerImage's Tag"
      type: "string"
      immutable: false
      required: true
