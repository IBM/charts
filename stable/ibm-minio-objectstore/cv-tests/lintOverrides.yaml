ignoreMessages:
  # The default values does not accessSercretName, thus installation fails on required check.
  - '\[ERROR\] render error in .* error calling include: template: ibm-minio-objectstore/templates/_helpers.tpl.* executing "minio.accessSecretName" .* required: Specify non empty minioAccessSercret \(or minioAccessSecretTemplateName\) value..*'
