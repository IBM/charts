# begin_generated_IBM_copyright_prolog                             
#                                                                  
# This is an automatically generated copyright prolog.             
# After initializing,  DO NOT MODIFY OR MOVE                       
# **************************************************************** 
# Licensed Materials - Property of IBM                             
# 5724-Y95                                                         
# (C) Copyright IBM Corp.  2018, 2019    All Rights Reserved.      
# US Government Users Restricted Rights - Use, duplication or      
# disclosure restricted by GSA ADP Schedule Contract with          
# IBM Corp.                                                        
#                                                                  
# end_generated_IBM_copyright_prolog                               
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: "{{ .Release.Name }}-ops"
  labels:
{{- include "streams.defaultLabels" . | indent 4 }}
    app.kubernetes.io/name: "{{ .Release.Name }}-ops"

spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: "{{ .Release.Name }}-ops"
      release: {{ .Release.Name }}
  podManagementPolicy: Parallel
  replicas: 1
  
  template:
    metadata:
      labels:
{{- include "streams.defaultLabels" . | indent 8 }}
{{- include "streams.serviceability" . | indent 8 }}
        app.kubernetes.io/name: "{{ .Release.Name }}-ops"
      annotations:
{{- include "streams.metering" . | indent 8 }}
        
    spec:
      affinity:
{{- include "streams.nodeaffinity" . | indent 8 }}
     
{{- include "streams.podGeneralSecurityPolicies" . | indent 6 }}
      securityContext:
{{- include "streams.streamsopsPodSecurityContext" . | indent 8 }}  
        
      terminationGracePeriodSeconds: 1

      serviceAccountName: {{ template "streams.instanceServiceAccountName" . }}
      
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.image.pullSecrets }}
      {{- end }}
      
      volumes:
{{- include "streams.opsVolumes" . | indent 8 }}        
        {{- if .Values.mkinstance.volumes }}
{{ toYaml .Values.mkinstance.volumes | indent 8 }}
        {{- end }}      
        
      containers:
        - name: streams-ops
          {{- if .Values.image.prefix }}
          image: "{{ .Values.image.prefix }}/{{ .Values.image.operations }}:{{ .Values.image.operationsTag }}"
          {{- else }}
          image: "{{ .Values.image.operations }}:{{ .Values.image.operationsTag }}"
          {{- end }}
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
          
          securityContext:
{{- include "streams.containerSecurityContext" . | indent 12 }} 
                
          env:
{{- include "streams.basicEnv" . | indent 12 }}
{{- include "streams.controllerSecurityEnv" . | indent 12 }}            
{{- include "streams.ssoSecurityEnv" . | indent 12 }}
            {{- if .Values.mkinstance.env }}
{{ toYaml .Values.mkinstance.env | indent 12 }}
            {{- end }}
            
          command: ["/bin/bash", "-c", "sleep infinity"]
          
          livenessProbe:
            exec:
              command:
              - ls 
              - {{ include "streams.installdir" . }}
            initialDelaySeconds: 60
            periodSeconds: 60
            timeoutSeconds: 15
            successThreshold: 1
            failureThreshold: 2
            
          readinessProbe:
            exec:
              command:
              - ls 
              - {{ include "streams.installdir" . }}
            initialDelaySeconds: 0
            periodSeconds: 15
            timeoutSeconds: 10
            successThreshold: 1
            failureThreshold: 2
            
          volumeMounts:
{{- include "streams.opsVolumeMounts" . | indent 12 }}            
            {{- if .Values.mkinstance.volumeMounts }}
{{ toYaml .Values.mkinstance.volumeMounts | indent 12 }}
            {{- end }}
            
          resources:
            requests:
              cpu: {{ .Values.resources.operationsCpu | default "100m" }}
              memory: {{ .Values.resources.operationsMemory | default "1Gi" }}            
            limits:
              cpu: {{ .Values.resources.operationsCpuLimit | default "1" }}
              memory: {{ .Values.resources.operationsMemoryLimit | default "2Gi" }}
            
