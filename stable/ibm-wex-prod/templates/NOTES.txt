{{- include "sch.config.init" (list . "ibm-wex-prod.sch.chart.config.values") -}}
{{- $compUiName := .sch.chart.components.wex.ui.name }}

{{ .Release.Name }} is ready to use. {{ .Release.Name }} is an instance of the {{ .Chart.Name }} chart.

Use https://<external ip>:{{ .Values.general.service.externalPort }} to access the application.
"external ip" can be retrieved by following command.

kubectl get service --namespace {{ .Release.Namespace }} {{ include "sch.names.fullCompName" (list . $compUiName ) }} -o jsonpath="{.spec.externalIPs}"

When multiple IPs are retrieved, pick up an IP which can be accessed outside of IBM Cloud Private cluster.
