apiVersion: v1
kind: ConfigMap
metadata: 
    name: {{ template "ibm-datapower-nonprod.fullname" . }}-config
    labels:
        app.kubernetes.io/name: {{ template "ibm-datapower-nonprod.fullname" . }}
        helm.sh/chart: {{ .Chart.Name}}-{{ .Chart.Version | replace "+" "_" }}
        release: "{{ .Release.Name }}"
        app.kubernetes.io/instance: "{{ .Release.Name }}"
        app.kubernetes.io/managed-by: "{{ .Release.Service }}"
data:
{{- include "ibm-datapower-nonprod.defaultDomainConfig" . | indent 4 }}
{{- include "ibm-datapower-nonprod.healthCheckConfig" . | indent 4 }}
{{- $configData := print "ibm-datapower-nonprod." .Values.patternName "Config" }}
{{- include $configData . | indent 4 }}
