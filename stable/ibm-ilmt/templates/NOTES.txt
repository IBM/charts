When configuring VM Manager Tool, use external port value under the Services section.

You can also get external port value from command line by executing following command: kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "ilmt.name" . }}