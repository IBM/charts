apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ilmt.name" . }}-server-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: "{{ include "ilmt.name" . }}-server-test"
    image: "{{ .Values.images.datacollector.repository }}:{{ .Values.images.datacollector.tag }}"
    command: ["sh", "-c", "exit 0"]
  restartPolicy: Never
