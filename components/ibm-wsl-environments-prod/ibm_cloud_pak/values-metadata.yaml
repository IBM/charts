license:
  __metadata:
    label: "setlicense"
    description: "license flag"

global:
  __metadata:
    label: "setglobal"
    description: "global scope"
  dockerRegistryPrefix:
    __metadata:
      label: "setdockerregistryprefix"
      description: "the docker registry prefix"
  cloudpakInstanceId:
    __metadata:
      label: "setcloudpakInstanceId"
      description: "the cloudpak Instance Id"
  currentModuleArch:
    __metadata:
      label: "setcurrentModuleArch"
      description: "target node architecture"
  replicaCount:
    __metadata:
      label: "setreplicacount"
      description: "replication count"
  cloudpakInstanceId:
    __metadata:
      label: "setcloudpakinstanceid"
      description: "cloud pak instance id"

uninstallJob:
  __metadata:
    label: "setuninstalljob"
    description: "uninstall job image details"
  image:
    repository:
      __metadata:
        label: "setrepository"
        description: "which repository to use"
    tag:
      __metadata:
        label: "settag"
        description: "which tag to use"

ibm-wsl-environments-api:
  __metadata:
    label: "setibmwslenvironmentsapi"
    description: "name to be used for chart labels"
ibm-wsl-environments-ui:
  __metadata:
    label: "setibmwslenvironmentsui"
    description: "name to be used for chart labels"

versions:
  __metadata:
    label: "setversions"
    description: "version definitions"
  environmentsApi:
    __metadata:
      label: "setenvironmentsapi"
      description: "environments api version"
  environmentsUi:
    __metadata:
      label: "setenvironmentsui"
      description: "environments ui version"
