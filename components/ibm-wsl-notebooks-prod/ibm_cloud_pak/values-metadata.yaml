license:
  __metadata:
    label: "setlicense"
    description: "license flag"

global:
  __metadata:
    label: "setglobal"
    description: "global scope"
  dockerRegistryPrefix:
    __metadata:
      label: "setdockerregistryprefix"
      description: "the docker registry prefix"
  cloudpakInstanceId:
    __metadata:
      label: "setcloudpakInstanceId"
      description: "the cloudpak Instance Id"
  currentModuleArch:
    __metadata:
      label: "setcurrentModuleArch"
      description: "target node architecture"
  replicaCount:
    __metadata:
      label: "setreplicacount"
      description: "replication count"
  cloudpakInstanceId:
    __metadata:
      label: "setcloudpakinstanceid"
      description: "cloud pak instance id"

initJob:
  __metadata:
    label: "setinitjob"
    description: "init job image details"
  image:
    repository:
      __metadata:
        label: "setrepository"
        description: "which repository to use"
    tag:
      __metadata:
        label: "settag"
        description: "which tag to use"
  scripts:
    version:
      __metadata:
        label: "setversion"
        description: "the scripts version"
  channels:
    version:
      __metadata:
        label: "setversion"
        description: "the channel version"

uninstallJob:
  __metadata:
    label: "setuninstalljob"
    description: "uninstall job image details"
  image:
    repository:
      __metadata:
        label: "setrepository"
        description: "which repository to use"
    tag:
      __metadata:
        label: "settag"
        description: "which tag to use"

versions:
  __metadata:
    label: "setversions"
    description: "version definitions"
  notebooksConverter:
    __metadata:
      label: "setnotebooksconverter"
      description: "jupyter notebooks converter version"
  notebooksJobManager:
    __metadata:
      label: "setnotebooksjobmanager"
      description: "notebooks job manager version"
  wdpNotebooksApi:
    __metadata:
      label: "setwdpnotebooksapi"
      description: "notebooks api version"
  wsNotebooksUi:
    __metadata:
      label: "setwsnotebooksui"
      description: "notebooks ui version"
