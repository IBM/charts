replicaCount:
  __metadata:
    name: "name"
    label: "label"
    description: "description"

arch:
  __metadata:
    name: "name"
    label: "label"
    description: "description"

global:
  __metadata:
    name: "name"
    label: "label"
    description: "description"
  dockerRegistryPrefix:
    __metadata:
      name: "name"
      label: "label"
      description: "description"
  sch:
    enabled:
      __metadata:
        name: "name"
        label: "label"
        description: "description"

image:
  __metadata:
    name: "name"
    label: "label"
    description: "description"
  dockerImage:
    __metadata:
      name: "name"
      label: "label"
      description: "description"
  tag:
    __metadata:
      name: "name"
      label: "label"
      description: "description"
  pullPolicy:
    __metadata:
      name: "name"
      label: "label"
      description: "description"

service:
  __metadata:
    name: "name"
    label: "label"
    description: "description"
  type:
    __metadata:
      name: "name"
      label: "label"
      description: "description"

resources:
  __metadata:
    name: "name"
    label: "label"
    description: "description"
  addon:
    requests:
      cpu:
        __metadata:
          name: "name"
          label: "label"
          description: "description"
      memory:
        __metadata:
          name: "name"
          label: "label"
          description: "description"
  serviceProvider:
    requests:
      cpu:
        __metadata:
          name: "name"
          label: "label"
          description: "description"
      memory:
        __metadata:
          name: "name"
          label: "label"
          description: "description"

serviceProvider:
  __metadata:
    name: "name"
    label: "label"
    description: "description"
  image:
    dockerImage:
      __metadata:
        name: "name"
        label: "label"
        description: "description"
    tag:
      __metadata:
        name: "name"
        label: "label"
        description: "description"
    pullPolicy:
      __metadata:
        name: "name"
        label: "label"
        description: "description"

  service:
    type:
      __metadata:
        name: "name"
        label: "label"
        description: "description"
    httpsPort:
      __metadata:
        name: "name"
        label: "label"
        description: "description"
    httpsPortName:
      __metadata:
        name: "name"
        label: "label"
        description: "description"
