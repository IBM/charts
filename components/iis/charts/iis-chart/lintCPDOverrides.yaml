overrides:
  - reason: "False positives as we aren't doing the Cloud Pak"
    rule: UsedParametersAreDefined
    severity: ERROR
  - reason: "False positives as we aren't doing the Cloud Pak"
    rule: ValidateMetadataSchema
    severity: ERROR
  - reason: "False positives as we aren't doing the Cloud Pak"
    rule: ReleaseNotesHasSuggestedSections
    severity: ERROR
  - reason: "False positives as we aren't doing the Cloud Pak"
    rule: CustomPSPInReadme
    severity: ERROR
  - reason: "False positives as we aren't doing the Cloud Pak"
    rule: NoHostIPC
    severity: ERROR