images:
- image: cassandra-x86_64:3.11.8
  references:
  - repository: cassandra-x86_64:3.11.8
    pull-repository: ${DOCKER_REGISTRY}/cassandra-x86_64:3.11.8
    pull-authorization:
      username:
        env: ARTIFACTORY_USERNAME
      password:
        env: ARTIFACTORY_PASSWORD
