apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}-db2u-instance
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  {{ if .Values.database.db2InstancePwd }}
  password:  {{ .Values.database.db2InstancePwd | b64enc | quote }}
  {{ else }}
  password: {{ randAlphaNum 10 | b64enc | quote }}  
  {{ end }}
