apiVersion: v1
kind: Pod
metadata:
  name: "test-connection"
spec:
  restartPolicy: Never
  containers:
  - name: create-nfs-subfolder
    image: '{{ .Values.images.tools.image.repository }}:{{ .Values.images.tools.image.tag }}{{ template "platform" . }}'
    {{- include "root.containers.securityContext" . | indent 4 }}
    command: ['/bin/sh']
    args:
      - -cx
      - |
        echo this is a test

