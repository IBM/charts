# Deploy if oketi-nfs storage class exist
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: db2wh-test-pvc
spec:
  storageClassName: "oketi-nfs"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 40Gi
