kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .cv.release }}-pvc
spec:
  storageClassName: "manual"
  selector:
    matchExpressions:
      - {key: "env", operator: In, values: ["development"]}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
storage: 40Gi