kind: PersistentVolume
apiVersion: v1
metadata:
  name: {{ .cv.release }}-pv
  labels:
    env: development
spec:
  storageClassName: "manual"
  capacity:
    storage: 40Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: {{ .cv.nfsIp }}
    path: {{ .cv.nfsPath }}
