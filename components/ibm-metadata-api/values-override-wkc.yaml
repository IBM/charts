containers:
  api:
    image:
      registry: docker-registry.default.svc:5000/zen
      pullPolicy: Always
  engine:
    image:
      registry: docker-registry.default.svc:5000/zen
      pullPolicy: Always
