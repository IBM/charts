# TODO: specify valid values
# charts:
# - archive: file:../../ibm-watson-discovery-operator-prod-0.1.0.tgz
#   repository-keys:
#   - global.image.repository

images:
# TODO: use valid image tag
- image: discovery-operator:main
  references:
  - repository : discovery-operator:main
    pull-repository: us.icr.io/discovery_borg/discovery-operator:main
    platform:
      os: linux
      architecture: amd64
