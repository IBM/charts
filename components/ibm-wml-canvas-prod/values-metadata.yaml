global:
  __metadata:
    label: "global settings"
    description: "Parameter settings for global variables"
  cloudpakInstanceId:
    __metadata:
      label: "cloudpak InstanceId"
      description: "the settings for cloudpak Instance Id"
  image:
    __metadata:
      label: "image settings"
      description: "Parameter settings for image"
    pullPolicy:
      __metadata:
        label: "image pullPolicy"
        description: "Parameter settings for image pullPolicy"

  annotations:
    __metadata:
      label: "pod annotations"
      description: "the settings for pod annotations"
    cloudpakName:
      __metadata:
        label: "annotations cloudpakName"
        description: "Parameter settings for annotations cloudpakName"
    cloudpakId:
      __metadata:
        label: "annotations cloudpakId"
        description: "Parameter settings for annotations cloudpakId"
    productMetric:
      __metadata:
        label: "annotations productMetric"
        description: "Parameter settings for annotations productMetric"
    productChargedContainers:
      __metadata:
        label: "annotations productChargedContainers"
        description: "Parameter settings for annotations productChargedContainers"
    productID:
      __metadata:
        label: "annotations productID"
        description: "Parameter settings for annotations productID"
    productName:
      __metadata:
        label: "annotations productName"
        description: "Parameter settings for annotations productName"
    productVersion:
      __metadata:
        label: "annotations productVersion"
        description: "Parameter settings for annotations productVersion"
    productCloudpakRatio:
      __metadata:
        label: "annotations productCloudpakRatio"
        description: "Parameter settings for annotations productCloudpakRatio"

  clusterDomain:
    __metadata:
      label: "cluster Domain"
      description: "the cluster domain settings"

  addOnPodLabels:
    __metadata:
      label: "add on pod labels"
      description: "the addOn pod labels settings"
    addOnId:
      __metadata:
        label: "ID for addon"
        description: "the settings for addonId"
    icpApp:
      __metadata:
        label: "app name"
        description: "app name settings for addon"
    spssApp:
      __metadata:
        label: "spss app name"
        description: "spss app name settings for addon"

  ibmProduct:
    __metadata:
      label: "IBM Product"
      description: "default IBM Production settings"

  buildVersion:
    __metadata:
      label: "build version"
      description: "default addon build version settings"

  flowSession:
    __metadata:
      label: "flow session"
      description: "the settings for flow session container"
    cpuLimits:
      __metadata:
        label: "cpu limits"
        description: "the settings for cpu limit of flow session pod"
    memLimits:
      __metadata:
        label: "memory limits"
        description: "the settings for memory limit of flow session pod"

  runAsUser:
    __metadata:
      label: "runAsUser settings"
      description: "Parameter settings for runAsUser ID"
  viewer:
    __metadata:
      label: "viewer settings"
      description: "Parameter settings for viewer role"
    sa:
      __metadata:
        label: "viewer sa settings"
        description: "Parameter settings for viewer sa"
  editor:
    __metadata:
      label: "editor settings"
      description: "Parameter settings for editor role"
    sa:
      __metadata:
        label: "editor sa settings"
        description: "Parameter settings for editor sa"

  watsonStudio:
    __metadata:
      label: "watsonStudio settings"
      description: "Parameter settings for watsonStudio"
    url:
      __metadata:
        label: "url settings"
        description: "the nginx url value"
    operatingEnvironment:
      __metadata:
        label: "operating Environment"
        description: "the operating environment, could be local, cloud and etc"
    deploymentTarget:
      __metadata:
        label: "deployment Target"
        description: "the deployment target, should use cp4d"
    trustSelfSignedCertificates:
      __metadata:
        label: "trust SelfSigned Certificates"
        description: "trusted SelfSigned Certificates settings"
  auth:
    __metadata:
      label: "authentication settings"
      description: "Parameter settings for authentication"
    icp4d:
      __metadata:
        label: "auth icp4d settings"
        description: "the icp4d authentication settings"
      enabled:
        __metadata:
          label: "icp4d enablement"
          description: "settings for icp4d authentication enablement "
      callbackUrl:
        __metadata:
          label: "callBack URL"
          description: "the settings for callBack URL"
      authorizationUrl:
        __metadata:
          label: "authorization URL"
          description: "the settings for authorization URL"
      publicKeyUrl:
        __metadata:
          label: "publicKey URL"
          description: "the settings for publicKey URL"
      logoutUrl:
        __metadata:
          label: "logout URL"
          description: "the settings for logout URL"
  nginx:
    __metadata:
      label: "nginx settings"
      description: "Parameter settings for nginx"
    enabled:
      __metadata:
        label: "nginx enablement"
        description: "the settings for nginx enablement"
    importSSLCertificate:
      __metadata:
        label: "import SSL Certificate "
        description: "settings for import SSL Certificate "
      enabled:
        __metadata:
          label: "ssl enablement"
          description: "settings for ssl certification enablement"
      certificateSecret:
        __metadata:
          label: "certificate Secret"
          description: "settings for certificate Secret"
        name:
          __metadata:
            label: "certificate Secret name "
            description: "the settings for certificate Secret name"
        key:
          __metadata:
            label: "certificate Secret key "
            description: "the settings for certificate Secret key"
    internalSSLCertificate:
      __metadata:
        label: "internal SSL Certificate "
        description: "settings for internal SSL Certificate "
      fileName:
        __metadata:
          label: "file name"
          description: "the file name of the SSL certification"
      mountFolder:
        __metadata:
          label: "mount folder"
          description: "the settings of the mount folder"
  docker:
    __metadata:
      label: "docker setting"
      description: "the settings for docker images"
    useNameSpace:
      __metadata:
        label: "use namespace"
        description: "the settings for use namespace"
    useImagePullSecret:
       __metadata:
         label: "use image pull secret"
         description: "the setting for using image pull secret"

  redis:
    __metadata:
      label: "redis setting"
      description: "the settings for redis"
    enabled:
      __metadata:
        label: "redis enablement"
        description: "the settings for redis enablement"
    configMap:
       __metadata:
         label: "redis configMap"
         description: "the setting for redis configMap"

  modelViewer:
    __metadata:
      label: "model view"
      description: "the settings for modeler viewer"
    url:
      __metadata:
        label: "modelViewer URL"
        description: "the settings for modelViewer URL"
  canvasApi:
    __metadata:
      label: "canvas Api"
      description: "the settings for canvas Api"
    url:
      __metadata:
        label: "canvasApi URL"
        description: "the settings for canvasApi URL"
  commonApi:
    __metadata:
      label: "common api"
      description: "the settings for common api"
    url:
      __metadata:
        label: "commonApi URL"
        description: "the settings for commonApi URL"
  commonUI:
    __metadata:
      label: "common UI"
      description: "the settings for common UI"
    url:
      __metadata:
        label: "commonUI URL"
        description: "the settings for common UI URL"
  wml:
    __metadata:
      label: "wml settings"
      description: "the settings for WML"
    url:
      __metadata:
        label: "WML URL"
        description: "the settings for WML URL"
  flowApi:
    __metadata:
      label: "flow api settings"
      description: "the settings for flow api microservice"
    nginxPath:
      __metadata:
        label: "nginx path"
        description: "the settings for flow api nginx path"
  modelerFlowApi:
    __metadata:
      label: "modeler flow api settings"
      description: "the settings for modeler flow api microservice"
    nginxPath:
      __metadata:
        label: "nginx path"
        description: "the settings for modeler flow api nginx path"
    url:
      __metadata:
        label: "Modeler Flow API URL"
        description: "the settings for Modeler Flow API URL"

flowSession:
  __metadata:
    label: "flow session image"
    description: "Parameter settings for flow session image"
  image:
    __metadata:
      label: "flow-sesion image settings"
      description: "the flow session image name settings"
    repository:
      __metadata:
        label: "flow-session image repository"
        description: "the flow session image repository setting"
    tag:
      __metadata:
        label: "flow-session image tag"
        description: "the flow session image tag setting "

flow-ui:
  __metadata:
    label: "flow ui image"
    description: "Parameter settings for flow ui image"
  image:
    __metadata:
      label: "flow-ui image settings"
      description: "the flow UI image name settings"
    repository:
      __metadata:
        label: "flow-ui image repository"
        description: "the flow UI image repository setting"
    tag:
      __metadata:
        label: "flow-ui image tag"
        description: "the flow UI image tag setting"

flow-api:
  __metadata:
    label: "flow api image"
    description: "Parameter settings for flow api image"
  image:
    __metadata:
      label: "image settings"
      description: "the flow api image name settings"
    repository:
      __metadata:
        label: "image repository"
        description: "the flow api image repository setting"
    tag:
      __metadata:
        label: "image tag"
        description: "the flow api setting image tag"

preInstaller:
  __metadata:
    label: "pre-installer image settings"
    description: "Parameter settings for pre-installer image"
  image:
    __metadata:
      label: "pre-installer image settings"
      description: "the pre-installer image name settings"
    repository:
      __metadata:
        label: "image repository"
        description: "the pre-installer image repository setting"
    tag:
      __metadata:
        label: "image tag"
        description: "the pre-installer setting image tag"

fullnameOverride:
  __metadata:
    label: "fullname settings"
    description: "full name override settings"

nameOverride:
  __metadata:
    label: "name settings"
    description: "the name override settings"

modeler-flow-api:
  __metadata:
    label: "modeler-flow-api image"
    description: "Parameter settings for modeler-flow-api image"
  image:
    repository:
      __metadata:
        label: "modeler-flow-api image repository"
        description: "the modeler-flow-api image repository setting"
    tag:
      __metadata:
        label: "modeler-flow-api image tag"
        description: "the modeler-flow-api image tag setting "

runtimes:
  __metadata:
    label: "runtime settings"
    description: "the settings for runtime pre-pull pods"
  workerNodes:
    __metadata:
      label: "worker nodes"
      description: "the settings for number of the worker nodes"
