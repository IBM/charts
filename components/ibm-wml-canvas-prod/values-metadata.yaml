global:
  __metadata:
    label: "global settings"
    description: "Parameter settings for global variables"
  architecture:
    __metadata:
      label: "architecture settings"
      description: "Parameter settings for platform architecture"
  annotations:
    cloudpakName:
      __metadata:
        label: "annotations cloudpakName"
        description: "Parameter settings for annotations cloudpakName"
    cloudpakId:
      __metadata:
        label: "annotations cloudpakId"
        description: "Parameter settings for annotations cloudpakId"
    cloudpakVersion:
      __metadata:
        label: "annotations cloudpakVersion"
        description: "Parameter settings for annotations cloudpakVersion"
    productMetric:
      __metadata:
        label: "annotations productMetric"
        description: "Parameter settings for annotations productMetric"
    productChargedContainers:
      __metadata:
        label: "annotations productChargedContainers"
        description: "Parameter settings for annotations productChargedContainers"
    productID:
      __metadata:
        label: "annotations productID"
        description: "Parameter settings for annotations productID"
    productName:
      __metadata:
        label: "annotations productName"
        description: "Parameter settings for annotations productName"
    productVersion:
      __metadata:
        label: "annotations productVersion"
        description: "Parameter settings for annotations productVersion"
  runAsUser:
    __metadata:
      label: "runAsUser settings"
      description: "Parameter settings for runAsUser ID"
  viewer:
    sa:
      __metadata:
        label: "viewer sa settings"
        description: "Parameter settings for viewer sa"
  editor:
    sa:
      __metadata:
        label: "editor sa settings"
        description: "Parameter settings for editor sa"
  billing:
    enabled:
      __metadata:
        label: "Billing enable settings"
        description: "Billing enable"
    serviceId:
      __metadata:
        label: "Billing ServiceId settings"
        description: "Billing servicdID settings"
  activityTracker:
    enabled:
      __metadata:
        label: "ActivityTracker enable settings"
        description: "ActivityTracker enable"
    region:
      __metadata:
        label: "ActivityTracker region settings"
        description: "ActivityTracker region settings"
    project:
      __metadata:
        label: "ActivityTracker project settings"
        description: "ActivityTracker project settings"
    url:
      __metadata:
        label: "ActivityTracker url settings"
        description: "ActivityTracker url settings"
  rabbitmq:
    enabled:
      __metadata:
        label: "rabbitmq enable settings"
        description: "rabbitmq enable settings"

  watsonStudio:
    url:
      __metadata:
        label: "watsonStudio url settings"
        description: "watsonStudio url settings"
    operatingEnvironment:
      __metadata:
        label: "watsonStudio operatingEnvironment settings"
        description: "watsonStudio operatingEnvironment settings"
    deploymentTarget:
      __metadata:
        label: "watsonStudio deploymentTarget settings"
        description: "watsonStudio deploymentTarget settings"
    trustSelfSignedCertificates:
      __metadata:
        label: "watsonStudio trustSelfSignedCertificates settings"
        description: "watsonStudio trustSelfSignedCertificates settings"
  cloudant:
    enabled:
      __metadata:
        label: "cloudant enabled settings"
        description: "cloudant enabled settings"
  newRelic:
    enabled:
      __metadata:
        label: "newRelic enabled settings"
        description: "newRelic enabled settings"
  auth:
    iam:
      __metadata:
        label: "auth IAM settings"
        description: "auth IAM settings"
    icp4d:
      __metadata:
        label: "auth icp4d settings"
        description: "auth icp4d settings"

