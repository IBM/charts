# This file allows to create some dummy secrets and configs to test the chart.
# those secrets are normally created by Watson Studio install that is a pre-requisite for this add-on.
# this test file allows to create dummy ones to test the chart standalone
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-ha-configmap
data:
  redis-url: d2hhdGV2ZXI= # whatever  
