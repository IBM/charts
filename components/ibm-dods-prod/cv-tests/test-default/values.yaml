global:
  architecture: "amd64"
  serviceAccount: # default service account in tests
  #dockerRegistryPrefix: registry.ng.bluemix.net/ws_do_dev # our dev bluemix registry
  dockerRegistryPrefix: hyc-icpcontent-docker-local.artifactory.swg-devops.com/dods # Cloudpack CICD registry

ddScenarioUi:
  noRedis: true # disable redis in tests to be able to run standalone without Watson Studio installed
  replicas: 1 # one single replica in tests

ddScenarioApi:
  replicas: 1 # one single replica in tests

ddCognitive:
  replicas: 1 # one single replica in tests