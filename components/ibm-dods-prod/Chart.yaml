apiVersion: v1
name: ibm-dods-prod
# description is used as productName in resource management and licensing annotations, 
# and shall match the display name of the product in addons.json metadata
description: IBM Decision Optimization
# chart version (used by helm), including fixpack number
version: 3.5.0
# app version: must be same version as cp4d module
appVersion: 3.5.0
# minimum requirements
tillerVersion: '>=2.9.0'
kubeVersion: '>=1.11.0'
# we support install from this version of ICP4D:
icp4dVersion: ">=2.0.0"
# we support upgrade from this version of the add-on:
upgradeVersion: 2.1.0
icon: https://datascience.ibm.com/graphics/landing/logo.svg
maintainers:
 - name: IBM Analytics - Decision Optimization
keywords:
  - Commercial
  - RHOCP
  - Analytics
  - amd64
  - ppc64le
  
  
