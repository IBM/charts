ConfigMap resource defined in `placeholder.yaml` is just fake resource because Helm neehook.
least one resource to be defined. This chart only run job to upload models into local object storage
and it is executed as `pre-install` hook.
