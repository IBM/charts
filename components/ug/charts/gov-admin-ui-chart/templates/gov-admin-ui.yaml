
# Licensed Materials - Property of IBM
# 5724-Q36
# Copyright IBM Corp. 2018
# US Government Users Restricted Rights - Use, duplication or disclosure
# restricted by GSA ADP Schedule Contract with IBM Corp.
#

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.adminUI.app_name }}
  labels:
     app: {{ .Values.adminUI.app_name }}
     chart: "{{ .Chart.Name }}"
     release: "{{ .Release.Name }}"
     heritage: "{{ .Release.Service }}"
     app.kubernetes.io/name: {{ .Values.adminUI.app_name }}
     helm.sh/chart: {{ .Chart.Name }}
     app.kubernetes.io/managed-by: {{ .Release.Service }}
     app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  selector:
     app: {{ .Values.adminUI.app_name }}
  ports:
  - name: http
    protocol: TCP
    port: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.adminUI.app_name }}
  labels:
     app: {{ .Values.adminUI.app_name }}
     chart: "{{ .Chart.Name }}"
     release: "{{ .Release.Name }}"
     heritage: "{{ .Release.Service }}"
     app.kubernetes.io/name: {{ .Values.adminUI.app_name }}
     helm.sh/chart: {{ .Chart.Name }}
     app.kubernetes.io/managed-by: {{ .Release.Service }}
     app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.adminUI.app_name }}
  template:
    metadata:
      labels:
        app: {{ .Values.adminUI.app_name }}
        chart: "{{ .Values.adminUI.chartName }}"
        release: "{{ .Release.Name }}"
        heritage: "{{ .Release.Service }}"
        app.kubernetes.io/name: {{ .Values.adminUI.app_name }}
        helm.sh/chart: {{ .Chart.Name }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        {{- if .Values.global.podLabels }}
{{ tpl (toYaml .Values.global.podLabels) . | trim | indent 8 }}
        {{- end }}
      annotations:
      {{- include "product-metering" . | indent 8 }}
    spec:
      hostNetwork: false
      hostPID: false
      hostIPC: false
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: "app"
                operator: In
                values:
                - {{ .Values.adminUI.app_name }}
            topologyKey: "kubernetes.io/hostname"
       {{- include "nodeaffinity" . | indent 6 }}
      containers:
        - name: {{ .Values.adminUI.app_name }}
          image: {{ if .Values.global.dockerRegistryPrefix }}{{ trimSuffix "/" .Values.global.dockerRegistryPrefix }}/{{ end }}{{ .Values.adminUI.repository }}:{{ .Values.adminUI.tag }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          command: [ "/bin/sh", "-c", "--" ]
          args: [ "sed -i 's/<title>Information Governance Administration<\\/title>/<title>IBM Cloud Private for Data<\\/title>/g' /app/dist/index.html;
                   sed -i 's/content=\"featureShowInZen\" value=\"off\"/content=\"featureShowInZen\" value=\"on\"/g' /app/dist/index.html;
                   npm start;"
                ]
          resources:
            requests:
              memory: "{{ .Values.adminUI.requests.memory }}"
              cpu: "{{ .Values.adminUI.requests.cpu }}"
            limits:
              memory: "{{ .Values.adminUI.limits.memory }}"
              cpu: "{{ .Values.adminUI.limits.cpu }}"
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            readOnlyRootFilesystem: false
            capabilities:
              drop:
              - ALL
          env:
          - name: RUN_TYPE
            value: {{.Values.global.ibmProduct | quote | upper }}
          ports:
            - containerPort: 3000
          readinessProbe:
           tcpSocket:
            port: 3000
           initialDelaySeconds: 5
          livenessProbe:
           tcpSocket:
            port: 3000
           initialDelaySeconds: 60
      securityContext:
        runAsNonRoot: true
        {{- if .Values.adminUI.runAsUser }}
        runAsUser: {{ .Values.adminUI.runAsUser }}
        {{- end }}
        {{- if .Values.adminUI.fsGroup }}
        fsGroup: {{ .Values.adminUI.fsGroup }}
        {{- end }}
      {{- if .Values.serviceAccountName }}
      serviceAccountName: {{ .Values.serviceAccountName }}
      {{- end }}
      {{- include "image-secret" . | indent 6 }}
