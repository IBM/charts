# workaround for old cp4d installer not setting useDynamicProvisioning correctly
global:
  persistence:
    useDynamicProvisioning: true

# rabbitmq
wdp-rabbitmq:
  serviceAccount:
    create: false
    name: default

