dependencies:
  - name: ibm-postgresql
    # Note that we DO NOT use vanila datastores. The datastore chart have been modified.
    # Do not use datastores from repositories. 
    version: "1.6.1"
    alias: config
    condition: global.postgres.create
