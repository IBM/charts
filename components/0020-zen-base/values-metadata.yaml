global:
  __metadata:
    label: "Global Settings"
    description: "Global settings for all components"
  persistence:
    __metadata:
      label: "persistence configuration"
      description: "persistence configuration for storage class, dynamic provisioning"
    storageClassName:
      __metadata:
        label: "storage class name"
        description: "storage class name to override"
  userHomePVC:
    __metadata:
      label: "user-home pvc configuration"
      description: "configuration of user-home pvc"
    persistence:
      __metadata:
        label: "user-home pvc persistence settings"
        description: "persistence settings of user-home pvc"
      storageClassName: 
        __metadata:
          label: "storage class name"
          description: "storage class name to use"
  docker_registry_prefix:
    __metadata:
      label: "url of docker registry"
      description: "prefix comprising of namespace"