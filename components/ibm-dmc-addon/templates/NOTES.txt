DMC is delivered as an interconnected set of pods and kubernetes services. It's a kind of base service of CP4D.

To get the CP4D URL by running below command.

1. oc login
2. input openshift user and credentials
3. run `oc get route` to get CP4D url
4. login CP4D url
