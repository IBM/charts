images:
- image: wdp-connect-connector:3.0.342
  references:
  - repository: wdp-connect-connector:3.0.342
    pull-repository: ${DOCKER_REGISTRY}/wdp-connect-connector:3.0.342
    pull-authorization:
      username:
        env: ARTIFACTORY_USERNAME
      password:
        env: ARTIFACTORY_PASSWORD
