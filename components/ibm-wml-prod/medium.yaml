commands:
  - scale --replicas=2 deployment wml-deployment-envoy
  - scale --replicas=2 deployment wmltraining
  - scale --replicas=2 deployment wml-repositoryv4
  - scale --replicas=2 statefulset wml-deployment-manager
  - scale --replicas=2 statefulset wml-deployment-agent
