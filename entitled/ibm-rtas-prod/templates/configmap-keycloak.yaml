apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gateway.fullname" . }}-keycloak-realm
  labels:
{{ include "gateway.labels" . | indent 4 }}
data:
  realm.json: |-
{{ .Files.Get "files/realm.json" | indent 4}}
