{{- if and .Values.intercepts.clusterRole.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "execution.fullname" . }}-intercepts-{{ .Release.Namespace }}
  labels:
{{ include "execution.labels" . | indent 4 }}
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["get","list"]
{{- end -}}
