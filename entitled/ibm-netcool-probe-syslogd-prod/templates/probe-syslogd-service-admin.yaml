{{- include "sch.config.init" (list . "probe.sch.chart.config.values") -}}
{{ $admin :=  printf "%s" "admin" -}}
{{- $compName :=  .sch.chart.components.probe.name -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sch.names.fullName" (list .) }}-{{ $admin }}
  labels:
{{ include "sch.metadata.labels.standard" (list . $admin) | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: syslogd-httpd
  selector:
    app.kubernetes.io/name: {{ include "sch.names.appName" (list .) }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: {{ $compName }}