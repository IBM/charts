{{- include "sch.config.init" (list . "sch.chart.config.values") -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "sch.names.fullName" (list .) }}-ltpa-creation-role-binding
  labels:
{{ include "sch.metadata.labels.standard" (list . "ums") | indent 4 }}
roleRef:
  kind: Role
  apiGroup: rbac.authorization.k8s.io
  name: {{ include "sch.names.fullName" (list .) }}-ltpa-creation-role
subjects:
- kind: ServiceAccount
  name: {{ include "sch.names.fullName" (list .) }}-ltpa-creation-service-account
  namespace: {{ .Release.Namespace }}
