apiVersion: v1
kind: ServiceAccount
metadata:
  name: cws-{{ RELEASE }}
  namespace: {{ NAMESPACE }}
  labels:
    app: {{ RELEASE }}-paiemaster
    chart: "ibm-powerai-enterprise-prod"
    heritage: "Tiller"
    release: "{{ RELEASE }}"
