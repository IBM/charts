# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*~
# Various IDEs
.project
.idea/
*.tmproj
cv-tests/
ppa-specs/
# Exclude from helm package but to include in PPA.
ibm_cloud_pak/pak_extensions
# Exclude manifest that is used to generate PPA only.
ibm_cloud_pak/manifest.yaml