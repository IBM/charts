{{- if not .Values.probe.snmpv3.secretName -}}
{{- include "sch.config.init" (list . "probe.sch.chart.config.values") -}}
{{- $compName :=  .sch.chart.components.probe.name -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sch.names.fullName" (list . ) }}
  labels:
{{ include "sch.metadata.labels.standard" (list . $compName) | indent 4 }}
type: Opaque
data:
  mttrapd.conf: {{if .Values.probe.snmpv3.users}}{{  include "snmpv3users" ( .Values.probe.snmpv3.users ) | b64enc  }}{{else}}""{{end}}
{{ end }}