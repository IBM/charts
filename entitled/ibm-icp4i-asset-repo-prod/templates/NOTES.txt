{{- include "sch.config.init" (list . ) }}
This instance of the ICP4I Asset Repository should be accessed through the ICP4I Platform Navigator, but can be accessed directly via https://{{ .Values.global.tls.hostname }}/{{ .Values.global.tls.ingresspath }}/data/catalogs