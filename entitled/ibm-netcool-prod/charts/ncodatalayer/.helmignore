# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
cv-tests/
# Common backup files
*.swp
*.bak
*.tmp
*~
# Various IDEs
.project
.idea/
*.tmproj

LICENSE
LICENSES/*
licenses/*
L_*.zip

cv-tests/
ppa-specs/

#  Debug files generated during build
helmtemplate.yaml

# Files pulled in from ibm-sch dependency
requirements.lock
