images:
- image: is-en-compute-image:b489-develop-b1286
  references:
  - repository: is-en-compute-image:b489-develop-b1286
    pull-repository: ${DOCKER_REGISTRY}/is-en-compute-image:b489-develop-b1286
    pull-authorization:
      username:
        env: ARTIFACTORY_USERNAME
      password:
        env: ARTIFACTORY_PASSWORD
- image: ds-entitlement-image:3.0.1
  references:
  - repository: ds-entitlement-image:3.0.1
    pull-repository: ${DOCKER_REGISTRY}/ds-entitlement-image:3.0.1
    pull-authorization:
      username:
        env: ARTIFACTORY_USERNAME
      password:
        env: ARTIFACTORY_PASSWORD
