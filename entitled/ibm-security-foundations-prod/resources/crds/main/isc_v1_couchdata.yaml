apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: couchdata.isc.ibm.com
spec:
  group: isc.ibm.com
  names:
    kind: CouchData
    listKind: CouchDataList
    plural: couchdata
    singular: couchdata
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CouchData is the Schema for the couchdata API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CouchDataSpec defines the desired state of CouchData
            properties:
              data:
                description: data to insert as yaml. either data or json should be
                  specified
                type: object
                x-kubernetes-preserve-unknown-fields: true
              database:
                description: Database to insert record
                type: string
              id:
                description: Record id to create/update
                type: string
              json:
                description: data to insert as json. either data or json should be
                  specified
                type: string
              doctype:
                description: document type
                type: string
                enum:
                - doc
                - design
                - consoleapp
            required:
            - database
            - id
            type: object
          status:
            description: FoundationStatus defines the observed state of Foundation
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    additionalPrinterColumns:
    - name: STATE
      type: string
      jsonPath: .status.conditions[0].type
    - name: AGE
      type: date
      jsonPath: .metadata.creationTimestamp
    - name: PROCESSED
      type: date
      jsonPath: .status.conditions[0].lastTransitionTime
    subresources:
      status: {}
