kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: spark-master221
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    run: spark-master221-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      run: spark-master221-deployment-pod
  template:
    metadata:
      labels:
        app: spark
        chart: "{{ .Chart.Name }}"
        release: "{{ .Release.Name }}"
        heritage: "{{ .Release.Service }}"
        component: spark-master221
        run: spark-master221-deployment-pod
      annotations:
        productName: IBM Data Science Experience Local
        productID: IBMDataScienceExperienceLocal_120_perpetual_00000
        productVersion: 1.2.0.3
    spec:
      terminationGracePeriodSeconds: 0
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                  - amd64
      {{- if .Values.global.baseInstaller }}
      nodeSelector:
        is_compute: "true"
      {{- end }}
      containers:
      - name: spark-master221-container
        image: {{ .Values.spark221.image.repository }}:{{ .Values.spark221.image.tag }}
        imagePullPolicy: {{ default "IfNotPresent" .Values.image.pullPolicy | quote }}
        command: ["bash"]
        args: ["-c", "umask 0 ; /scripts/start_master.sh"]
        env:
        - name: APACHE_SPARK_VERSION
          value: "2.2.1"
        ports:
        - containerPort: 7077
        - containerPort: 8080
        readinessProbe:
          exec:
            command:
            - ls
            - /
          failureThreshold: 3
          initialDelaySeconds: 30
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 1
        livenessProbe:
          exec:
            command:
            - ls
            - /
          failureThreshold: 3
          initialDelaySeconds: 30
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 1
        volumeMounts:
        - mountPath: /user-home
          name: user-home-mount
      volumes:
      {{- include "user-home-pvc" .  | indent 6 }}
