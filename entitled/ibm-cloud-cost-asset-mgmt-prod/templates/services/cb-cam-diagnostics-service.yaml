apiVersion: v1
kind: Service
metadata:
  name: cb-cam-diagnostics
  labels:
    name: cb-cam-diagnostics
{{ include "cam.labels" .  | indent 4 }}
spec:
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  selector:
    name: cb-cam-diagnostics
