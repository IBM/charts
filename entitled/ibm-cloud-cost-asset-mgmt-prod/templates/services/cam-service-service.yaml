apiVersion: v1
kind: Service
metadata:
  name: camservice
  labels:
    name: camservice
{{ include "cam.labels" .  | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    name: camservice
