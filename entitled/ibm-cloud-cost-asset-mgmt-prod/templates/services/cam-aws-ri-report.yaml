apiVersion: v1
kind: Service
metadata:
  name: cam-aws-ri-report
  labels:
    name: cam-aws-ri-report    
{{ include "cam.labels" .  | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: "http"
      port: 5000
      targetPort: "http"
  selector:
    name: cam-aws-ri-report
