apiVersion: v1
kind: Service
metadata:
  name: cam-orch-v2
  labels:
    name: cam-orch-v2    
{{ include "cam.labels" .  | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: port4
      port: 4081
      targetPort: 4081
      protocol: TCP
  selector:
    name: cam-orch-v2
