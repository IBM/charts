apiVersion: v1
kind: Service
metadata:
  name: cam-task-mgr
  labels:
    name: cam-task-mgr
{{ include "cam.labels" .  | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: port44
      port: 4041
      targetPort: 4041
      protocol: TCP
  selector:
    name: cam-task-mgr
