# camui-host
#   Read by dash.gravitant.net to create URL links to CAM GUI for QA/PROD
#   ** DO NOT DELETE **
apiVersion: v1
kind: ConfigMap
metadata:
  name: camui-host
  labels:
{{ include "cam.labels" .  | indent 4 }}
data:
  ui.host: "{{ .Values.uiHost }}"
