apiVersion: v1
kind: Pod
metadata:
  name: cb-core-ibus-delivery-agent-test
  labels:
    name: cb-core-ibus-delivery-agent-test
{{ include "core.test-labels" .  | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  affinity:
{{ include "core.nodeaffinity" . | indent 4 }}
{{ include "core.securityContext" .  | indent 2 }}
{{ include "core.imagePullSecrets" .  | indent 2 }}
  containers:
  - name: cb-core-ibus-delivery-agent-test
  {{- template "isIcp" . -}}
  {{- if not (.Values.isIcp) }}
    image: {{ template "core.readConfig" (list . "cb_credential_service") }}
  {{ else }}
    image: {{ template "coreicp.readConfig" (list . "cb_credential_service" .Values.imageCredentials.registry) }}
  {{ end }}
{{ include "core.container.securityContext" . | indent 4  }}
    command: ["sh", "-c", "wget -O - http://cb-core-ibus-delivery-agent:5001"]
  restartPolicy: Never
