apiVersion: v1
kind: Pod
metadata:
  name: core-notification-api-test
  labels:
    name: core-notification-api-test
{{ include "core.test-labels" .  | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  affinity:
{{ include "core.nodeaffinity" . | indent 4 }}
{{ include "core.securityContext" .  | indent 2 }}
{{ include "core.imagePullSecrets" .  | indent 2 }}
  containers:
  - name: core-notification-api-test
    {{- template "isIcp" . -}}
    {{- if not (.Values.isIcp) }}
    image: {{ template "core.readConfig" (list . "cb_notification_api") }}
    {{ else }}
    image: {{ template "coreicp.readConfig" (list . "cb_notification_api" .Values.imageCredentials.registry) }}
    {{ end }}
{{ include "core.container.securityContext" . | indent 4  }}
    command: ["sh", "-c", "wget -O - http://cb-notification-api:5056/email/health"]
  restartPolicy: Never
