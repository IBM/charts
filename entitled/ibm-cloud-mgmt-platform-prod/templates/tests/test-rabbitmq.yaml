apiVersion: v1
kind: Pod
metadata:
  name: cb-rabbitmq-test
  labels:
    name: cb-rabbitmq-test
{{ include "core.test-labels" .  | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  affinity:
{{ include "core.nodeaffinity" . | indent 4 }}
{{ include "core.securityContext" .  | indent 2 }}
{{ include "core.imagePullSecrets" .  | indent 2 }}
  containers:
  - name: cb-rabbitmq-test
  {{- template "isIcp" . -}}
  {{- if not (.Values.isIcp) }}
    image: {{ template "core.readConfig" (list . "cb_rabbitmq") }}
  {{ else }}
    image: {{ template "coreicp.readConfig" (list . "cb_rabbitmq" .Values.imageCredentials.registry) }}
  {{ end }}
{{ include "core.container.securityContext" . | indent 4  }}
    command: ["sh", "-c", "wget -O - http://rabbit1:15672"]
  restartPolicy: Never
