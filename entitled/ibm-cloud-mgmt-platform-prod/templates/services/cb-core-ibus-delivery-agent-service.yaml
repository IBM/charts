apiVersion: v1
kind: Service
metadata:
  name: cb-core-ibus-delivery-agent
  labels:
{{ include "core.labels" .  | indent 4 }}
spec:
  ports:
    - name: "5001"
      port: 5001
      targetPort: 5000
  selector:
    name: cb-core-ibus-delivery-agent
