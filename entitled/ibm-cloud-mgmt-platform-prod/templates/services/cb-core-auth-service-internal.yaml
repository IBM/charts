apiVersion: v1
kind: Service
metadata:
  name: cb-core-auth-internal
  labels:
{{ include "core.labels" .  | indent 4 }}
spec:
  ports:
    - name: "4001"
      port: 4001
      targetPort: 4001
  selector:
    name: cb-core-auth
