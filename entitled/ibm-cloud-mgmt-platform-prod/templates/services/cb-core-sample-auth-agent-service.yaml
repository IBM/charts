apiVersion: v1
kind: Service
metadata:
  name: cb-core-sample-auth-agent
  labels:
{{ include "core.labels" .  | indent 4 }}
spec:
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
  selector:
    io.kompose.service: cb-core-sample-auth-agent
