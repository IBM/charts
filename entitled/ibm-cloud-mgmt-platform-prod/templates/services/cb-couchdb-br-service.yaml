{{- if eq (.Values.enableBackups|quote|lower) "\"true\"" -}}
apiVersion: v1
kind: Service
metadata:
  name: cb-couchdb-br
  labels:
{{ include "core.labels" .  | indent 4 }}
spec:
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
  selector:
    name: cb-couchdb-br
{{ end }}
