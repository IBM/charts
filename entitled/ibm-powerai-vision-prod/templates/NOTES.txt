{{- $root := . -}}
{{- range .Values.ingress.hosts }}
  {{- if . }}
Access the PowerAI Vision UI at the following URL:

https://{{ . }}/{{ template "shortname" $ }}/

  {{- else }}
Find the PowerAI Vision UI URL by running the following commands:

export NODE_IP=$(kubectl get ing {{ template "shortname" $ }}-ing --namespace {{ $.Release.Namespace }} -o jsonpath="{.status.loadBalancer.ingress[0].ip}")
echo https://${NODE_IP}/{{ template "shortname" $ }}/
  {{- end }}
{{- end }}
