# IBM_SHIP_PROLOG_BEGIN_TAG
# *****************************************************************
#
# Licensed Materials - Property of IBM
#
# (C) Copyright IBM Corp. 2018. All Rights Reserved.
#
# US Government Users Restricted Rights - Use, duplication or
# disclosure restricted by GSA ADP Schedule Contract with IBM Corp.
#
# *****************************************************************
# IBM_SHIP_PROLOG_END_TAG
# We'll use a template as this gets deployed twice.
{{ $d := merge (dict) . }}
# Add any custom variables we need
{{ $shortname := include "shortname" . }}
{{ $_ := set $d "namePrefix" (printf "%s-video-test-rabmq" $shortname) }}
{{ $_ := set $d "runMountPath" "run/video-detection-test-data/rabbitmq" }}
# Include the template and scope it appropriately
{{ include "vision-video-rabbitmq-deployment-tpl" $d }}