Following components have been installed by this helm. You can visit them with URLs:
{{- if .Values.baStudio.install }}
Business Automation Studio
    https://{{ .Values.global.baStudio.hostname }}:{{ .Values.global.baStudio.port }}/BAStudio
{{- end }}
{{- if .Values.appengine.install }}
Automation Application Designer
    https://{{ .Values.global.appEngine.hostname }}:{{ .Values.global.appEngine.port }}/
{{- end }}
{{- if .Values.resourceRegistry.install }}
Resource Registry
    Visit by gRPC with hostname {{ .Values.global.resourceRegistry.hostname }} and port {{ .Values.global.resourceRegistry.port }}
{{- end }}

You can verify the install by using command:
helm test {{ .Release.Name }}