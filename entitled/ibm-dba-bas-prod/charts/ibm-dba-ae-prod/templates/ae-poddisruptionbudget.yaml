###############################################################################
#
# Licensed Materials - Property of IBM
#
# (C) Copyright IBM Corp. 2019. All Rights Reserved.
#
# US Government Users Restricted Rights - Use, duplication or
# disclosure restricted by GSA ADP Schedule Contract with IBM Corp.
#
###############################################################################
{{ include "sch.config.init" (list . "sch.appengine.config.values") }}
{{ $label := (include "sch.metadata.labels.standard" (list . "pdb")) }}
{{ $labelWorload := (include "sch.metadata.labels.standard" (list . "deployment")) }}
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ include "sch.names.fullCompName" (list . "pdb-deployment") }}-{{ randNumeric 3 }}
  labels:
{{ $label | indent 4 }}
spec:
  minAvailable: {{ if gt (int .Values.replicaCount) 2 }}2{{ else }}1{{ end }}
  selector: 
    matchLabels:
{{ $labelWorload | indent 6 }}