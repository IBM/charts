You can visit Business Automation Studio with external URL:
https://{{ .Values.global.baStudio.hostname }}:{{ .Values.global.baStudio.port}}/BAStudio

And you can verify the cluster available with command:
helm test {{ .Release.Name }}
