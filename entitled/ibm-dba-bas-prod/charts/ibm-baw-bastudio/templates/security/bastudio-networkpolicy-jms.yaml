{{- include "sch.config.init" (list . "sch.bastudio.config.values") -}}
{{ $label := (include "sch.metadata.labels.standard" (list . "npolicy-jms")) }}
{{ $labelBastudio := (include "sch.metadata.labels.standard" (list . "deployment")) }}
###############################################################################
#
# Licensed Materials - Property of IBM
#
# (C) Copyright IBM Corp. 2019. All Rights Reserved.
#
# US Government Users Restricted Rights - Use, duplication or
# disclosure restricted by GSA ADP Schedule Contract with IBM Corp.
#
###############################################################################
{{ $labelWorkload := (include "sch.metadata.labels.standard" (list . "jms")) }}
{{- $policyName := include "sch.names.fullCompName" (list . "npolicy-jms") -}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $policyName }}
  labels: 
{{ $label | indent 4 }}
spec:
  podSelector:
    matchLabels:
{{ $labelWorkload | indent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - ports:
    - port: 7286
    from:
    - podSelector:
        matchLabels:
{{ $labelBastudio | indent 10 }}