###############################################################################
#
# Licensed Materials - Property of IBM
#
# (C) Copyright IBM Corp. 2019. All Rights Reserved.
#
# US Government Users Restricted Rights - Use, duplication or
# disclosure restricted by GSA ADP Schedule Contract with IBM Corp.
#
###############################################################################
{{- include "sch.config.init" (list . "sch.bastudio.config.values") -}}
{{ $label := (include "sch.metadata.labels.standard" (list . "npolicy-test")) }}
{{ $labelWorkload := (include "sch.metadata.labels.standard" (list . "availability-test")) }}
{{- $policyName := include "sch.names.fullCompName" (list . "npolicy-test") -}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $policyName }}
  labels: 
{{ $label | indent 4 }}
spec:
  podSelector:
    matchLabels:
{{ $labelWorkload | indent 6 }}
  policyTypes:
  - Ingress
  - Egress
  egress:
  - to:
    ports:
    - protocol: TCP
      port: {{ .Values.global.baStudio.port }}
    - protocol: TCP
      port: 9443
    - protocol: UDP
      port: 53
