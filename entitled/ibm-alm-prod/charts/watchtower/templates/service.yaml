{{- include "sch.config.init" (list . "alm.sch.chart.config.values") -}}
apiVersion: v1
kind: Service
metadata:
  name: watchtower
  labels:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 8284
    targetPort: 8284
    protocol: TCP
    name: http-watchtower
  selector:
    app: watchtower
    release: {{ .Release.Name }}
