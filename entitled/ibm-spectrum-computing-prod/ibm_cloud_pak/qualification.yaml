prereqs:
  security:
    kubernetes:
     podSecurityPolicy:
      name: "ibm-privileged-psp"
    ibmCloudPrivate:
      installerRole:
        name: "ClusterAdministrator"
  k8sDistros:
    ibmCloudPrivate:
      semver: ">=1.11.0"
