apiVersion: v1
kind: Service
metadata:
  name: wml-scoring-rt-utils
  labels:
    app: wml-scoring-rt-utils
    chart: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  type: ClusterIP
  selector:
    app: wml-scoring-rt-utils
  ports:
  - protocol: TCP
    port: 16502
    targetPort: 16502
