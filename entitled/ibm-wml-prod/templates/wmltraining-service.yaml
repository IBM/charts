apiVersion: v1
kind: Service
metadata:
  name: wmltrainingservice
  labels:
    app: wmltraining
    chart: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - port: 13300
    protocol: TCP
    targetPort: 13300
  selector:
    app: wmltraining
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
