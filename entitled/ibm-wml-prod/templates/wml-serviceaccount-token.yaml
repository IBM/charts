apiVersion: v1
data:
  ca.crt: |
  namespace: |
  token: |

kind: ConfigMap
metadata:
  name: wmlscoringserviceaccount
  labels:
    app: wml-scoring
    chart: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
