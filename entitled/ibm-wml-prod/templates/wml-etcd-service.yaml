apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: wml-etcd
    chart: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  name: wml-etcd
spec:
  ports:
  - name: "2379"
    port: 2379
    protocol: TCP
    targetPort: 2379
  selector:
    app: wml-etcd
  type: ClusterIP
status:
  loadBalancer: {}
