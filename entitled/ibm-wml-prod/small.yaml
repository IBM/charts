commands:
  - scale --replicas=1 deployment wml-os-envoy
  - scale --replicas=1 deployment wmlrepository
  - scale --replicas=1 deployment wmltraining
  - scale --replicas=1 statefulset wml-scoring-rt-utils
  - |
    patch statefulset wml-os-manager --patch '{ "spec": { "replicas": 1, "template": { "spec": { "containers": [{ "env": [{ "name": "DEFAULT_DEPLOY_REPLICA", "value": "1" }], "name": "runtimemanagercontainer" }] } } } }'
