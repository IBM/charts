{{- if (.Capabilities.APIVersions.Has "route.openshift.io/v1") }}
{{- include "sch.config.init" (list . "ibm-ucdr-prod.sch.chart.config.values") -}}
{{- $fullName := include "sch.names.fullName" (list .) -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ $fullName }}-codestation
  labels:
{{ include "sch.metadata.labels.standard" (list . "route") | indent 4 }}
spec:
  host: {{ .Values.ingress.codestationhost | quote}}
  subdomain: ''
  to:
    kind: Service
    name: {{ $fullName | quote }}
    weight: 100
  port:
    targetPort: codestation
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
status:
  ingress: []
{{- end }}
