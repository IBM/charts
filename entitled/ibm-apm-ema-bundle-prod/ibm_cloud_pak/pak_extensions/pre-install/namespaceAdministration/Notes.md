The namespaceAdministration directory should contain any information, scripts, samples that apply at a namespace scope that need to be done before installing the chart.

Example:
- Create secret
- Create PersistentVolumeClaim
- Create imagePolicy