1. Test that NMS is alive using the following commands:
  echo "Port forward to models-server service on port 8000"
  kubectl port-forward svc/{{ .Release.Name }}-ibm-watson-nms-models-server 8000:8000 --namespace {{ .Release.Namespace }}

curl -k -X GET https://localhost:8000/health
