{{- include "sch.config.init" (list . .Values.schConfigName) -}}
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ include "sch.names.fullCompName" (list . "gateway-pdb") | quote }}
  labels:
{{ include "sch.metadata.labels.standard" (list . "gateway-pdb") | indent 4 }}
spec:
  {{- if include "gateway.booleanConvertor" (list .Values.autoscaling.enabled . ) }}
  minAvailable: {{ tpl (.Values.autoscaling.minReplicas | toString) . }}
  {{- else }}
  maxUnavailable: 0
  {{- end }}
  selector:
    matchLabels:
{{ include "sch.metadata.labels.standard" (list . "gateway") | indent 6 }}