{{- include "sch.config.init" (list . "osgflask.sch.chart.config.values") -}}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "sch.names.fullName" (list . ) }}
  labels:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4}}
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4}}
