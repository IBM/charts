{{- include "sch.config.init" (list . "osgui.sch.chart.config.values") -}}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "sch.names.fullName" (list . ) }}
  labels:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4 }}  
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
  selector:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4 }}
