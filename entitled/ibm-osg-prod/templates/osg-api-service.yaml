{{- include "sch.config.init" (list . "osgapi.sch.chart.config.values") -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sch.names.fullName" (list . ) | quote }}
  labels:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4 }}
