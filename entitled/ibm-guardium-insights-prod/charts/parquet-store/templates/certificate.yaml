{{- if .Values.global.insights.licenseAccept }}
apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: {{ include "parquet-store.fullname" . }}
  labels: 
{{ include "parquet-store.labels" . | indent 4 }}
spec:
  secretName: parquet-store-ca-gen
  duration: 8760h #365d/1y
  renewBefore: 240h # 10d
  commonName: {{ .Release.Namespace }}-parquet-store
  isCA: false
  keySize: 2048
  keyAlgorithm: rsa
  keyEncoding: pkcs8
  usages:
    - server auth
    - client auth
  dnsNames: 
  - {{ include "parquet-store.fullname" . }}
  - {{ .Release.Namespace }}-parquet-store
  - {{ .Release.Namespace }}-parquet-store.{{ .Release.Namespace }}
  issuerRef:
    name: {{ .Values.global.clusterIssuer }}
    kind: ClusterIssuer
{{- end }}