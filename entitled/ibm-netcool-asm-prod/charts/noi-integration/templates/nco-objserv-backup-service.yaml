{{- include "sch.config.init" (list . "asm.sch.chart.config.values") -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-objserv-backup
  labels:
{{ include "sch.metadata.labels.standard" (list . "") | indent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: {{ .Values.releaseName }}
    app.kubernetes.io/name: ncobackup
  ports:
    - name: bak-tds-port
      protocol: TCP
      port: 4100
