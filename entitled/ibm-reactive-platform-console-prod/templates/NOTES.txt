Thank you for installing Chart: {{ .Chart.Name }}-{{ .Chart.Version }}.

Your release is named {{ .Release.Name }}.

In order to run the following commands, you must first be logged into your IBM Cloud Private instance:

cloudctl login -a {ICP_URL} --skip-ssl-validation

To test this release, use the helm test command:

helm test {{ .Release.Name }} --cleanup

To get the URL for the IBM Reactive Platform UI, run the following commands:

export NODE_PORT={{ .Values.esConsoleExposePort }}
export CONSOLE_IP=$(kubectl -n kube-system get pod | grep nginx-ingress | cut -f1 -d' ' | xargs -I {} kubectl -n kube-system get pod {} -o yaml | grep publish-status-address | cut -f2 -d'=')

echo "Open your web browser to http://$CONSOLE_IP:$NODE_PORT"