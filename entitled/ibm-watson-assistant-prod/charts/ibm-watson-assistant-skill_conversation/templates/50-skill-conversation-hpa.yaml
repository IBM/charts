{{- if .Values.global.autoscaling.enabled }}
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: "{{ .Release.Name }}-skill-conversation-hpa"
  labels:
    app:       "ibm-watson-assistant-prod-1.3.0"
    chart:     "ibm-watson-assistant"
    component: "skill-conversation"
    heritage : "{{ .Release.Service }}"
    release:   "{{ .Release.Name }}"
spec:
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: "{{ .Release.Name }}-skill-conversation"
  minReplicas: {{ include "assistant.skillConversation.replicas" . }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  targetCPUUtilizationPercentage: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}