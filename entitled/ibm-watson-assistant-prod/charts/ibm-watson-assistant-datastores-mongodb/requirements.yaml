dependencies:
  - name: ibm-mongodb
    # Note that we DO NOT use clean datastores. The datastore chart have been modified (From version 1.3.2)
    # Do not use datastores from repositories. 
    version: 1.6.0
    alias: config
