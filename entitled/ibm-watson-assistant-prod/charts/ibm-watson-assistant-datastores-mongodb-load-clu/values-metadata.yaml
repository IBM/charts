image:
  __metadata:
      label: Image
      description: Docker Image
  repository:
    __metadata:
      label: Repository
      description: Docker image location
      type: string
      required: true
  name:
    __metadata:
      label: Name
      description: Name of the image in the repository
      type: string
      required: true
  tag:
    __metadata:
      label: Image Tag
      description: Docker image Tag
      type: string
      required: true

resources:
  __metadata:
    label: Resources
    description: Kubernetes resource allocations
  limits:
    cpu:
      __metadata:
        label: CPU limit
        description: Kubernetes CPU resource limit
        required: true
    memory:
      __metadata:
        label: Memory limit
        description: Kubernetes Memory resource limit
        required: true
  requests:
    cpu:
      __metadata:
        label: CPU request
        description: Kubernetes CPU resource request
        required: true
    memory:
      __metadata:
        label: Memory request
        description: Kubernetes Memory resource request
        required: true
