apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name}}-clu-load-mongo"
  labels:
    app: {{ .Release.Name}}-clu-load-mongo
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  load-clu-data.sh: |
{{ .Files.Get "scripts/load-clu-data.sh" | indent 4 }}