mongodb:
  config:
    persistentVolume:
      enabled: false

cos:
  minio:
    persistence:
      enabled: false


etcd:
  config:
    persistence:
      enabled: false

postgres:
  config:
    persistence:
      enabled: false
