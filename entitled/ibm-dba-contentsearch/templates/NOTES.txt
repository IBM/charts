1. Get the application URL by running these commands:

# Get the ClusterIP for the service
  export CLUSTER_IP=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.clusterIP}" services {{ template "ibm-dba-contentsearch.fullname" .  }})

# Get the Port for the service

  export PORT=$(kubectl get --namespace {{ .Release.Namespace }}  -o jsonpath="{.spec.ports[0].port}" services {{ template "ibm-dba-contentsearch.fullname" .  }})

  echo "Using this Cluster IP address and Port to configure CSS server in CPE: $CLUSTER_IP:$PORT"
