{{- include "sch.config.init" (list . "probe.kafka.sch.chart.config.values") -}}
{{- $compName :=  .sch.chart.components.probe.name -}}
You can check that the probe is running by sending a HTTP GET to the URL below:
http://{{ include "sch.names.fullName" (list .) }}:8081/probe/common

Publish messages to the following topic(s) and verify that the probe has received the messages:
{{ .Values.messagebus.kafka.connection.topics }}


