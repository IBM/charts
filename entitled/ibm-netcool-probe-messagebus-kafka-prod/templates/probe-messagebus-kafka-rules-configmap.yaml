{{- if not .Values.messagebus.probe.rulesConfigmap }}
{{- include "sch.config.init" (list . "probe.kafka.sch.chart.config.values") -}}
{{- $compName :=  .sch.chart.components.probe.name -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sch.names.fullName" (list .) }}-rules
  labels:
{{ include "sch.metadata.labels.standard" (list . $compName) | indent 4 }}
data:
{{- include "ibm-netcool-probe-messagebus-kafka-prod.probeMessageBusKafkaRules" . | indent 2 }}
{{- end }}
