{{- include "sch.config.init" (list . "probe.kafka.sch.chart.config.values") -}}
{{- $compName :=  .sch.chart.components.probe.name -}}
{{- $configName :=  .sch.chart.components.probe.configmap.name -}}
{{- $configMapName := include "sch.names.fullCompName" (list . $configName) -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $configMapName }}
  labels:
{{ include "sch.metadata.labels.standard" (list . $compName) | indent 4 }}  
data:
{{ include "ibm-netcool-probe-messagebus-kafka-prod.probeMessageBusKafkaConfig" . | indent 2 }}
