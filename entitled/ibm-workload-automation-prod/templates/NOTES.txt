Your IBM Workload Automation is starting.
{{ if .Values.global.enableServer }}
{{- if .Values.waserver.server.ingress.enabled }}
{{- if .Values.waserver.server.ingress.hostname }}
To verify if the server is successfully installed use the following URL: https://{{ .Values.waserver.server.ingress.hostname }}/twsd
{{- else }}
To verify if the server is successfully installed use the following URL: https://{{ .Release.Name }}-waserver.mycluster.proxy/twsd	
{{- end }}
{{- else }}
To verify if the server is successfully installed use the following URL https://{{ template "waMdm.ServiceName" . }}:31116/twsd from a browser inside the cluster. 
To test it from outside the cluster you can use the "kubectl port-forward statefulset/{{ .Release.Name }}-waserver <local_port>:31116" and use to the following URL: https://localhost:<local_port>/twsd 
{{- end }}
{{- end }}
{{- if .Values.global.enableConsole }}
{{- if .Values.waconsole.console.ingress.enabled }}
{{- if .Values.waconsole.console.ingress.hostname }}
To launch Dynamic Workload Console use the following URL: https://{{ .Values.waconsole.console.ingress.hostname }}/console
{{- else }}
To verify if the console is successfully installed use the following URL: https://{{ .Release.Name }}-waconsole.mycluster.proxy/console
{{- end	}}
{{- else }}
To verify if the console is successfully installed use the following URL https://{{ .Release.Name }}-waconsole:9443/console from a browser inside the cluster.
To test it from outside the cluster you can use the "kubectl port-forward statefulset/{{ .Release.Name }}-waconsole <local_port>:9443" and use to the following URL: https://localhost:<local_port>/console 
{{- end }}	
{{- end }}
{{- if .Values.global.enableAgent }}
To verify if the agent is connected to the Master Domain Manager, connect to the Dynamic Workload Console and list the available workstations or open a shell on the Master Domain Manager host and list the available workstations with the "conman sc" command.
{{- end }}