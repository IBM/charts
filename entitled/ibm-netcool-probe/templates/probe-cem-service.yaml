{{ if .Values.cemProbe.enabled }}
{{- include "sch.config.init" (list . "probe.sch.chart.config.values") -}}
{{- $compName :=  .sch.chart.components.cem.name -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sch.names.fullCompName" (list .  $compName) }}
  labels:
{{ include "sch.metadata.labels.standard" (list . $compName) | indent 4 }}
spec:
  type: {{ .Values.cemProbe.service.type }}
  ports:
    - port: {{ .Values.cemProbe.service.externalPort }}
      targetPort: 4080
      protocol: TCP
      name: webhook-cem
  selector:
    app.kubernetes.io/name: {{ include "sch.names.appName" (list .) }}
    app.kubernetes.io/component: {{ $compName }}    
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
