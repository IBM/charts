{{- if .Values.cemProbe.enabled -}}
{{- if .Values.cemProbe.poddisruptionbudget.enabled -}}
{{- include "sch.config.init" (list . "probe.sch.chart.config.values") -}}
{{- $compName :=  .sch.chart.components.cem.name -}}
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ include "sch.names.fullCompName" (list .  $compName) }}
  labels:
{{ include "sch.metadata.labels.standard" (list . $compName) | indent 4 }}
spec:
  minAvailable: {{ .Values.cemProbe.poddisruptionbudget.minAvailable }}
  selector:
    matchLabels:
      app.kubernetes.io/component: {{ $compName }}
      app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
{{- end -}}