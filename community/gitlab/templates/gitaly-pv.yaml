apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "fullname" . }}-gitaly-pv
spec:
  capacity:
    storage: 50Gi
  nfs:
    server: 9.30.181.217
    path: /export/gitlab
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
