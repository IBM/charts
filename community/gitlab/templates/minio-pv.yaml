apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "fullname" . }}-minio-pv
spec:
  capacity:
    storage: 10Gi
  nfs:
    server: 9.30.181.217
    path: /export/gitlab
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
