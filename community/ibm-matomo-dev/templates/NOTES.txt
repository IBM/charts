1. Get the Matomo URL:

{{- if contains "LoadBalancer" .Values.service.type }}

  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        Watch the status with: 'kubectl get svc --namespace {{ .Release.Namespace }} -w {{ template "matomo.fullname" . }}'

{{- $port:=.Values.service.port | toString }}
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ template "matomo.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo "Matomo URL: http://$SERVICE_IP{{- if ne $port "80" }}:{{ .Values.service.port }}{{ end }}/"
  echo "Matomo Admin URL: http://$SERVICE_IP{{- if ne $port "80" }}:{{ .Values.service.port }}{{ end }}/admin"

{{- else if contains "ClusterIP"  .Values.service.type }}

  echo "Matomo URL: http://127.0.0.1:8080/"
  echo "Matomo Admin URL: http://127.0.0.1:8080/admin"
  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ template "matomo.fullname" . }} 8080:{{ .Values.service.port }}

{{- else if contains "NodePort" .Values.service.type }}

  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ template "matomo.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "Matomo URL: http://$NODE_IP:$NODE_PORT/"
  echo "Matomo Admin URL: http://$NODE_IP:$NODE_PORT/admin"

{{- end }}
