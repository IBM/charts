apiVersion: v1
kind: Service
metadata:
  labels:
    role: webhook
  name: webhook
  namespace: knative-eventing
spec:
  ports:
  - port: {{ .Values.webhook.port }}
    targetPort: 443
  selector:
    role: webhook
