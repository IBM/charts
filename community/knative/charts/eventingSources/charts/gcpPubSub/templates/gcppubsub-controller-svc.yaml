apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: gcppubsub-controller-manager
  name: gcppubsub-controller
  namespace: knative-sources
spec:
  ports:
  - port: {{ .Values.gcppubsubController.port }}
  selector:
    control-plane: gcppubsub-controller-manager
