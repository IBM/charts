apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: controller
  namespace: knative-sources
spec:
  ports:
  - port: {{ .Values.controller.port }}
  selector:
    control-plane: controller-manager
