apiVersion: v1
kind: Service
metadata:
  labels:
    serving.knative.dev/release: devel
  name: zipkin
  namespace: istio-system
spec:
  ports:
  - name: http
    port: {{ .Values.zipkin.http.port }}
  selector:
    app: zipkin
