apiVersion: v1
kind: Service
metadata:
  labels:
    app: controller
    serving.knative.dev/release: devel
  name: controller
  namespace: knative-serving
spec:
  ports:
  - name: metrics
    port: {{ .Values.controller.metrics.port }}
    protocol: TCP
    targetPort: 9090
  selector:
    app: controller
