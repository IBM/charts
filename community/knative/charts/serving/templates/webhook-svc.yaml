apiVersion: v1
kind: Service
metadata:
  labels:
    role: webhook
    serving.knative.dev/release: devel
  name: webhook
  namespace: knative-serving
spec:
  ports:
  - port: {{ .Values.webhook.port }}
    targetPort: 443
  selector:
    role: webhook
