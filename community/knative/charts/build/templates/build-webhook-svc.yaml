apiVersion: v1
kind: Service
metadata:
  labels:
    role: build-webhook
  name: build-webhook
  namespace: knative-build
spec:
  ports:
  - port: {{ .Values.buildWebhook.port }}
    targetPort: 443
  selector:
    role: build-webhook
